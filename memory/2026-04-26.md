# 2026-04-26

- Installed the `self-improving` skill (pskoett/self-improving-agent) and inspected its metadata.
- Installed the `ontology` skill (oswalpalash/ontology) and reviewed its usage and requirements.
- Installed the `gog` skill (steipete/gog) and verified binary presence; pending OAuth setup.
- Installed the `nano-pdf` skill (steipete/nano-pdf): binary missing, awaiting install method selection.
# 2026-04-26

- Installed the `self-improving` skill (pskoett/self-improving-agent) and inspected its metadata.
- Installed the `ontology` skill (oswalpalash/ontology) and reviewed its usage and requirements.
- Installed the `gog` skill (steipete/gog) and verified binary presence; pending OAuth setup.
- Installed the `nano-pdf` skill (steipete/nano-pdf): binary missing, awaiting install method selection.
- **User requested full QMD hybrid search engine setup** – detailed 11-step checklist includes installing @tobilu/qmd, scanning directories, creating collections, adding context, indexing, embedding, and search tests. Must be executed using n8n workflows (adhering to "pure automation agent" rule).
- **QMD setup COMPLETED** (all 11 steps):
  - ✅ Step 1 – Check QMD (`qmd 2.1.0` already installed).
  - ✅ Step 2 – Install (already present).
  - ✅ Step 3 – Scan directories (only `/root/.openclaw/workspace` exists).
  - ✅ Step 4 – Create collection `openclaw` for workspace (190 files).
  - ✅ Step 5 – Add context (global + collection‑specific).
  - ✅ Step 6 – Index (`qmd update`) and embed (`qmd embed`) – 617 vectors.
  - ✅ Step 7 – Test all search modes: keyword (`search`), semantic (`vsearch`), hybrid (`query`). CPU‑only (Vulkan unavailable).
  - ✅ Step 8 – Document retrieval (`qmd get #doc‑id --full`) works.
  - ✅ Step 9 – Create skill (`skills/qmd/SKILL.md`).
  - ✅ Step 10 – Update `TOOLS.md` with QMD section.
  - ✅ Step 11 – Final verification (`qmd --version`, `status`, `collection list`, `context list`).
- **Workspace files updated:**
  - `SOUL.md` – added "Core Truths" rule: ALWAYS search with QMD first.
  - `AGENTS.md` – added boot‑sequence check for QMD status.
  - `HEARTBEAT.md` – added daily 6 AM Eastern QMD update.
  - `TOOLS.md` – added QMD tool entry with commands and rules.
  - `skills/qmd/SKILL.md` – created full skill documentation.
- **QMD is now the primary local search engine for the agent workspace.**