# 2026-05-30

## Durable Updates
- Installed and native-hardened the `polymarket-trade` skill from ClawHub. Native CLI wrapper is `polymarket` at `/root/.local/bin/polymarket`; skill path is `/root/.openclaw/workspace/skills/polymarket-trade/SKILL.md`. The skill is read-only market data plus local paper trading only, with no wallet/API-key/real-money execution. Verified `trending`, `search`, `movers`, `calendar`, `portfolio`, and `alerts`. Fixed a calendar pagination/filtering bug in `scripts/polymarket.py` so upcoming future resolutions return correctly.
- Verified the `github` skill was already installed at `/root/.openclaw/workspace/skills/github/SKILL.md`. Hardened its metadata so OpenClaw reports it as ready/native and automatically uses `gh` for GitHub issues, PRs, workflow runs, and API queries. `gh` exists at `/usr/bin/gh`, version `2.45.0`, authenticated as `tonybuildforsoftware0-ui`. End-to-end tests passed with `gh issue`, `gh pr`, `gh run`, and `gh api` against `cli/cli`.
- Installed and native-hardened `agent-browser-clawdbot`. OpenClaw skill name is `agent-browser`; skill path is `/root/.openclaw/workspace/skills/agent-browser-clawdbot/SKILL.md`; CLI is `/usr/bin/agent-browser`, version `0.27.0`; Chrome for Testing is installed at `/root/.agent-browser/browsers/chrome-149.0.7827.54/chrome`. `agent-browser doctor` reported 9 pass, 0 warn, 0 fail.
- Agent Browser permanent usage note: use `agent-browser` for browser-required workflows such as rendered pages, clicks/forms/login/session state/cookies, deterministic accessibility snapshots, tab workflows, isolated sessions, and network blocking/interception. After navigation or interaction, run `agent-browser wait --load networkidle` when appropriate, then `agent-browser snapshot -i --json`. Snapshot refs such as `@e1` are snapshot-local and stale after page changes. Save workflow/auth state under `~/.agent-browser/openclaw/<site-or-skill>/`; saved auth/cookie state is sensitive and must not be committed or pasted into logs.
- Agent Browser priority rule: prefer purpose-built APIs/skills first for stable read-only data (`github`/`gh`, `polymarket`, `tavily`, `summarize`, `web_fetch`). Use `agent-browser` when the page requires JavaScript, clicking/forms, cookies/auth/session reuse, browser rendering, or when an API/search result is insufficient.
- Agent Browser tests passed: real-site navigation/snapshot; Wikipedia search via parsed refs; login demo form submission; state save/load retaining login; two isolated sessions with no shared cookie state; network `route --abort` blocking a resource; scoped snapshot/extract; multi-tab switching/extraction; GitHub PR rendering after `gh`; Polymarket event rendering after `polymarket trending`.
- Agent Browser local gotchas: `openclaw skills info agent-browser-clawdbot` fails because installed internal skill name is `agent-browser`; use `openclaw skills info agent-browser`. In `agent-browser` v0.27.0, tab switching uses IDs like `t1`/`t2` or labels, not numeric `1`. `network route --abort` works; local tests showed `network route --body` registered but did not override `fetch()` responses, so verify mocking before relying on response-body overrides.
- Full agent-browser audit was written to `/root/.openclaw/workspace/memory/maintenance/agent-browser-system-audit-2026-05-30.md`; raw install/test logs were under `/tmp/agent-browser-install-step*.log` and `/tmp/agent-browser-test-*.log`.
- QMD indexing/embedding was refreshed after the new/updated skills. Known QMD warning remains: missing Vulkan support triggers a CPU fallback, but indexing/embedding completed successfully.
