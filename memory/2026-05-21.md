# Memory - 2026-05-21

## BigFuture reminder
- Tony wants to finish/check the College Board BigFuture $40,000 scholarship.
- I queued reminders for tonight and tomorrow evening.
- Useful links:
  - https://bigfuture.collegeboard.org/pay-for-college/bigfuture-scholarships
  - https://bigfuture.collegeboard.org/pay-for-college/bigfuture-scholarships/official-rules-class-2025
  - https://www.fastweb.com/
  - https://www.niche.com/colleges/scholarships/
  - https://scholarships360.org/scholarships/weird-scholarships/
  - https://www.careeronestop.org/Toolkit/Training/find-scholarships-detail.aspx?curPage=1&studyLevelfilter=Associate%20Degree&scholarshipId=9991168
  - https://hesc.ny.gov/find-aid/nys-grants-scholarships/excelsior-scholarship-program

## Second Gmail setup
- Tony wants a reminder to create OAuth for a second Gmail account for college monitoring and then send it here so it can be monitored too.

## Git essentials skill
- Installed the `git-essentials` skill from ClawHub and verified it is available locally.
- Confirmed the intended workflow is native `git` execution with the skill acting as the reference layer, not a wrapper.
- Tested the Git path on the real workspace and a temp repo: `git status`, `git log`, `git remote -v`, `git init`, `git add`, `git commit`, `git switch`, and `git stash` all worked as expected.

## Gmail OAuth blocker
- The second Gmail account for college/scholarship monitoring is `tonymasoudnyc@gmail.com`.
- OpenClaw’s `gog` OAuth flow hit Google error 403 `access_denied` because the app is still in testing and the account is not on the approved tester list.
- Manual OAuth via pasted redirect URL did not bypass the Google verification restriction.
- The next viable fix is to add `tonymasoudnyc@gmail.com` as a test user in the Google Cloud OAuth consent screen or use a verified OAuth client.

## Gmail OAuth manual-flow lesson
- Pasting the Google redirect URL back into the terminal does not bypass the OAuth consent-screen testing gate.
- For this OpenClaw setup, the only working path is still either approved tester access or a verified OAuth client.

## Gmail + AgentMail recovery
- Rebuilt the second Gmail account for `tonymasoudnyc@gmail.com` on a fresh `gog` file-keyring path with an intentionally empty `GOG_KEYRING_PASSWORD` so non-interactive agents can read it without prompting.
- Imported the college OAuth refresh token into the `college` client bucket and verified live Gmail search works with the normal `gog` command path.
- The old file-keyring backup for the main Gmail account still exists, but its password was not recoverable from local state, so the main mailbox will need a fresh OAuth sign-in before it can be revalidated.
- Verified AgentMail inbox access is live for `openclawmainemail@agentmail.to` and found recent college/scholarship messages from Stony Brook University and City College sources.

## Gmail reauth completed
- Reauthenticated `tony.guirguis.dell@gmail.com` through the manual `gog auth add ... --manual` flow and confirmed `gog auth list` shows both the `default` and `college` Gmail accounts.
- Verified the main mailbox now returns live Gmail search results, including a fresh Google security alert, so both Gmail accounts are currently working.

## Cron and heartbeat check
- Verified the live OpenClaw cron registry and system crontab on 2026-05-21.
- Current healthy jobs: `github-weekly-self-update`, `daily-brief-nyc-scholarship-stem-optimized`, `github-daily-drops-v4`, `daily-maintenance`, `daily-backup`, `mega-weekly-nyc-scholarship-stem-sweep`, `github-activity-monitor-v1`, `nyc-tech-events-scout`.
- Expected future/one-shot jobs are present and idle as expected: `nyssb-weekly-reminder` and `bigfuture-40k-reminder-2026-05-22`.
- The only live scheduler issue found was `gmail-4h-monitor`, which had 2 consecutive errors with `Agent couldn't generate a response`; its script/state path itself is intact, so this looks like an agent/runtime failure rather than a missing cron definition.
- Heartbeat and host automation are healthy: `cron.service`, `docker.service`, `telegraf.service`, and `ssh.service` are active; the heartbeat log is fresh and the host metrics show the gateway and monitoring containers running.
